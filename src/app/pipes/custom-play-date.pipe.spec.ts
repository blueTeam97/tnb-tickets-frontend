import { CustomPlayDatePipe } from './custom-play-date.pipe';

describe('CustomPlayDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPlayDatePipe();
    expect(pipe).toBeTruthy();
  });
});
