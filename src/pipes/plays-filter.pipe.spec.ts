import { PlaysFilterPipe } from './plays-filter.pipe';

describe('PlaysFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaysFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
